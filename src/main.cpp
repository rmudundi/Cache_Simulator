#include <iostream>
#include <cache.h>

int main(){
    //later: ask which type of mapping

    // Direct-mapping first
    Cache();
    //access addresses in trace file
    //read memories and process into caches
    //print stats or put stats into a folder



}